# Approach
Count occurrences across arrays (each value appears at most once per array). Values present in every array, in the order of the first array, form the LCS of sorted arrays.

# Complexity
Time O(total length). Space O(universe).
