# 1624. Largest Substring Between Two Equal Characters

Use the standard efficient approach implemented in `maxLengthBetweenEqualCharacters`. The implementation handles the required edge cases deterministically.

Time and space complexity follow the algorithm's maintained state and input traversal.
