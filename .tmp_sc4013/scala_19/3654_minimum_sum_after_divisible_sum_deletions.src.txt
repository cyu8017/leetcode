FOLDER: 3654_minimum_sum_after_divisible_sum_deletions
TITLE: 3654. Minimum Sum After Divisible Sum Deletions
URL: - **LeetCode:** [https://leetcode.com/problems/minimum-sum-after-divisible-sum-deletions/](https://leetcode.com/problems/minimum-sum-after-divisible-sum-deletions/)
CONFIG: {
  "class": "Solution",
  "method": "solve",
  "paramOrder": [
    "nums",
    "k"
  ],
  "types": null,
  "description": "Update method name and add cases to cases.json when implementing the solution."
}
SOURCE: Solution.java
================================================================================
// LeetCode 3654 - Minimum Sum After Divisible Sum Deletions
// https://leetcode.com/problems/minimum-sum-after-divisible-sum-deletions/

class Solution {
    public long minArraySum(int[] nums, int k) {
        int n = nums.length;
        int[] prefix = new int[n + 1];
        for (int i = 0; i < n; i++) prefix[i + 1] = (prefix[i] + nums[i]) % k;
        long inf = 1L << 62;
        long[] dp = new long[n + 1], best = new long[k];
        for (int i = 0; i < k; i++) best[i] = inf;
        best[0] = 0;
        for (int i = 1; i <= n; i++) {
            dp[i] = dp[i - 1] + nums[i - 1];
            if (best[prefix[i]] < dp[i]) dp[i] = best[prefix[i]];
            if (dp[i] < best[prefix[i]]) best[prefix[i]] = dp[i];
        }
        return dp[n];
    }
}
