FOLDER: 3641_longest_semi_repeating_subarray
TITLE: 3641. Longest Semi-Repeating Subarray
URL: - **LeetCode:** [https://leetcode.com/problems/longest-semi-repeating-subarray/](https://leetcode.com/problems/longest-semi-repeating-subarray/)
CONFIG: {
  "class": "Solution",
  "method": "solve",
  "paramOrder": [
    "nums",
    "k"
  ],
  "types": null,
  "description": "Update method name and add cases to cases.json when implementing the solution."
}
SOURCE: Solution.java
================================================================================
// LeetCode 3641 - Longest Semi-Repeating Subarray
// https://leetcode.com/problems/longest-semi-repeating-subarray/

import java.util.HashMap;
import java.util.Map;

class Solution {
    public int longestSubarray(int[] nums, int k) {
        Map<Integer, Integer> cnt = new HashMap<>();
        int ans = 0, cur = 0, l = 0;
        for (int r = 0; r < nums.length; r++) {
            if (cnt.merge(nums[r], 1, Integer::sum) == 2) cur++;
            while (cur > k) {
                if (cnt.merge(nums[l], -1, Integer::sum) == 1) cur--;
                l++;
            }
            ans = Math.max(ans, r - l + 1);
        }
        return ans;
    }
}
