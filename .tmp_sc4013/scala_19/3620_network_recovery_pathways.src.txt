FOLDER: 3620_network_recovery_pathways
TITLE: 3620. Network Recovery Pathways
URL: - **LeetCode:** [https://leetcode.com/problems/network-recovery-pathways/](https://leetcode.com/problems/network-recovery-pathways/)
CONFIG: {
  "class": "Solution",
  "method": "findMaxPathScore",
  "paramOrder": [
    "edges",
    "online",
    "k"
  ],
  "types": null,
  "description": "Update method name and add cases to cases.json when implementing the solution."
}
SOURCE: Solution.java
================================================================================
// LeetCode 3620 - Network Recovery Pathways
// https://leetcode.com/problems/network-recovery-pathways/

import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;
import java.util.PriorityQueue;

class Solution {
    private List<int[]>[] g;
    private boolean[] online;
    private long k;
    private int n;

    private boolean check(int mid) {
        int INF = Integer.MAX_VALUE / 2;
        int[] dist = new int[n];
        Arrays.fill(dist, INF);
        dist[0] = 0;
        PriorityQueue<int[]> pq = new PriorityQueue<>((a, b) -> Integer.compare(a[0], b[0]));
        pq.offer(new int[] {0, 0});
        while (!pq.isEmpty()) {
            int[] cur = pq.poll();
            int d = cur[0], u = cur[1];
            if ((long) d > k) return false;
            if (u == n - 1) return true;
            if (dist[u] < d) continue;
            for (int[] e : g[u]) {
                int v = e[0], w = e[1];
                if (w < mid) continue;
                int nd = d + w;
                if (nd < dist[v]) {
                    dist[v] = nd;
                    pq.offer(new int[] {nd, v});
                }
            }
        }
        return false;
    }

    public int findMaxPathScore(int[][] edges, boolean[] online, long k) {
        this.online = online;
        this.k = k;
        n = online.length;
        @SuppressWarnings("unchecked")
        List<int[]>[] gg = new ArrayList[n];
        g = gg;
        for (int i = 0; i < n; i++) g[i] = new ArrayList<>();
        int l = Integer.MAX_VALUE, r = 0;
        for (int[] e : edges) {
            int u = e[0], v = e[1], w = e[2];
            if (!online[u] || !online[v]) continue;
            g[u].add(new int[] {v, w});
            l = Math.min(l, w);
            r = Math.max(r, w);
        }
        if (l == Integer.MAX_VALUE) return -1;
        while (l < r) {
            int mid = (l + r + 1) >> 1;
            if (check(mid)) l = mid;
            else r = mid - 1;
        }
        return check(l) ? l : -1;
    }
}
