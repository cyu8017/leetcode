FOLDER: 3670_maximum_product_of_two_integers_with_no_common_bits
TITLE: 3670. Maximum Product of Two Integers With No Common Bits
URL: - **LeetCode:** [https://leetcode.com/problems/maximum-product-of-two-integers-with-no-common-bits/](https://leetcode.com/problems/maximum-product-of-two-integers-with-no-common-bits/)
CONFIG: {
  "class": "Solution",
  "method": "solve",
  "paramOrder": [
    "nums"
  ],
  "types": null,
  "description": "Update method name and add cases to cases.json when implementing the solution."
}
SOURCE: Solution.java
================================================================================
// LeetCode 3670 - Maximum Product of Two Integers With No Common Bits
// https://leetcode.com/problems/maximum-product-of-two-integers-with-no-common-bits/

class Solution {
    public long maxProduct(int[] nums) {
        int maxV = 0;
        for (int v : nums) if (v > maxV) maxV = v;
        int bitsN = 0;
        for (int x = maxV; x > 0; x >>= 1) bitsN++;
        if (bitsN == 0) bitsN = 1;
        int size = 1 << bitsN;
        int[] best = new int[size];
        for (int v : nums) if (v > best[v]) best[v] = v;
        for (int mask = 0; mask < size; mask++) {
            for (int b = 0; b < bitsN; b++) {
                if ((mask & (1 << b)) != 0) {
                    int sub = mask ^ (1 << b);
                    if (best[sub] > best[mask]) best[mask] = best[sub];
                }
            }
        }
        long ans = 0;
        for (int v : nums) {
            int comp = (size - 1) ^ v;
            if (best[comp] > 0) {
                long p = (long)v * best[comp];
                if (p > ans) ans = p;
            }
        }
        return ans;
    }
}
