FOLDER: 3660_jump_game_ix
TITLE: 3660. Jump Game IX
URL: - **LeetCode:** [https://leetcode.com/problems/jump-game-ix/](https://leetcode.com/problems/jump-game-ix/)
CONFIG: {
  "class": "Solution",
  "method": "solve",
  "paramOrder": [
    "nums"
  ],
  "types": null,
  "description": "Update method name and add cases to cases.json when implementing the solution."
}
SOURCE: Solution.java
================================================================================
// LeetCode 3660 - Jump Game IX
// https://leetcode.com/problems/jump-game-ix/

class Solution {
    public int[] maxValue(int[] nums) {
        int n = nums.length;
        int[] ans = new int[n], preMax = new int[n];
        preMax[0] = nums[0];
        for (int i = 1; i < n; i++) preMax[i] = Math.max(preMax[i - 1], nums[i]);
        int sufMin = Integer.MAX_VALUE / 2;
        for (int i = n - 1; i >= 0; i--) {
            if (preMax[i] > sufMin) ans[i] = ans[i + 1];
            else ans[i] = preMax[i];
            sufMin = Math.min(sufMin, nums[i]);
        }
        return ans;
    }
}
