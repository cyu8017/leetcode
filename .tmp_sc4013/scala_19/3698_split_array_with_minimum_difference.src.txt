FOLDER: 3698_split_array_with_minimum_difference
TITLE: 3698. Split Array With Minimum Difference
URL: - **LeetCode:** [https://leetcode.com/problems/split-array-with-minimum-difference/](https://leetcode.com/problems/split-array-with-minimum-difference/)
CONFIG: {
  "class": "Solution",
  "method": "solve",
  "paramOrder": [
    "nums"
  ],
  "types": null,
  "description": "Update method name and add cases to cases.json when implementing the solution."
}
SOURCE: Solution.java
================================================================================
// LeetCode 3698 - Split Array With Minimum Difference
// https://leetcode.com/problems/split-array-with-minimum-difference/

class Solution {
    public long splitArray(int[] nums) {
        int n = nums.length;
        long[] s = new long[n];
        boolean[] f = new boolean[n], g = new boolean[n];
        for (int i = 0; i < n; i++) { f[i] = true; g[i] = true; }
        s[0] = nums[0];
        for (int i = 1; i < n; i++) {
            s[i] = s[i - 1] + nums[i];
            f[i] = f[i - 1];
            if (nums[i] <= nums[i - 1]) f[i] = false;
        }
        for (int i = n - 2; i >= 0; i--) {
            g[i] = g[i + 1];
            if (nums[i] <= nums[i + 1]) g[i] = false;
        }
        long inf = Long.MAX_VALUE / 4;
        long ans = inf;
        for (int i = 0; i < n - 1; i++) {
            if (f[i] && g[i + 1]) {
                long s1 = s[i], s2 = s[n - 1] - s[i];
                ans = Math.min(ans, Math.abs(s1 - s2));
            }
        }
        return ans < inf ? ans : -1;
    }
}
