# 1629. Slowest Key

Use the standard efficient approach implemented in `slowestKey`. The implementation handles the required edge cases deterministically.

Time and space complexity follow the algorithm's maintained state and input traversal.
