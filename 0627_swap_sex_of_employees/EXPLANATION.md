# How We Solve Swap Sex of Employees

Flip `'m'` and `'f'` in one `UPDATE` using a `CASE` expression.

## Steps

1. Write a single update against `Salary`.
2. Set `sex` to the opposite enum value for every row.
