# How We Solve Big Countries

Select countries that are large by area or by population.

## Steps

1. Query `name`, `population`, and `area` from `World`.
2. Keep rows with `area >= 3000000` or `population >= 25000000`.
