=== 3457_eat_pizzas ===
class=Solution method=maxWeight params=['pizzas']
src_lang=cpp
---SOURCE---
// LeetCode 3457 - Eat Pizzas!
// https://leetcode.com/problems/eat-pizzas/

#include <vector>
#include <algorithm>

class Solution {
public:
    long long maxWeight(std::vector<int>& pizzas) {
        std::sort(pizzas.begin(), pizzas.end());
        int n = (int)pizzas.size();
        int days = n / 4;
        long long ans = 0;
        int oddDays = (days + 1) / 2;
        int evenDays = days / 2;
        int idx = n - 1;
        for (int i = 0; i < oddDays; i++) {
            ans += pizzas[idx];
            idx--;
        }
        for (int i = 0; i < evenDays; i++) {
            idx--;
            ans += pizzas[idx];
            idx--;
        }
        return ans;
    }
};

---END---