object Solution { def majorityElement(nums: Array[Int]): Int = { var candidate = 0; var count = 0; for (num <- nums) { if (count == 0) candidate = num; count += (if (num == candidate) 1 else -1) }; candidate } }