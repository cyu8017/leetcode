{
  "cases": [
    {
      "args": {
        "fn": "function add(b) { return this.a + b; } args = [{\"a\": 5}, 7]"
      },
      "expected": 12
    },
    {
      "args": {
        "fn": "function tax(price, taxRate) { return `The cost of the ${this.item} is ${price * taxRate}`; } args = [{\"item\": \"burger\"}, 10, 1.1]"
      },
      "expected": "The cost of the burger is 11"
    }
  ]
}
