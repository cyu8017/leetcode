# 1649. Create Sorted Array Through Instructions

Use the standard efficient approach implemented in `createSortedArray`. The implementation handles the required edge cases deterministically.

Time and space complexity follow the algorithm's maintained state and input traversal.
