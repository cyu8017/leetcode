folder=3279_maximum_total_area_occupied_by_pistons
class=Solution
method=maxArea
params=['height', 'positions', 'directions']
kind=None
source=solution.js
---SOURCE---
// LeetCode 3279 - Maximum Total Area Occupied by Pistons
// https://leetcode.com/problems/maximum-total-area-occupied-by-pistons/

var maxArea = function(height, positions, directions) {
    const n = positions.length;
    const pos = positions.slice();
    const dir = directions.split('');
    let best = 0;
    for (let t = 0; t <= 2 * height; t++) {
        let sum = 0;
        for (let i = 0; i < n; i++) sum += pos[i];
        if (sum > best) best = sum;
        for (let i = 0; i < n; i++) {
            if (dir[i] === 'U') {
                if (pos[i] === height) { dir[i] = 'D'; pos[i]--; }
                else pos[i]++;
            } else {
                if (pos[i] === 0) { dir[i] = 'U'; pos[i]++; }
                else pos[i]--;
            }
        }
    }
    return best;
};
