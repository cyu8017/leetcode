# 1616. Split Two Strings To Make Palindrome

Use the standard efficient approach implemented in `checkPalindromeFormation`. The implementation handles the required edge cases deterministically.

Time and space complexity follow the algorithm's maintained state and input traversal.
