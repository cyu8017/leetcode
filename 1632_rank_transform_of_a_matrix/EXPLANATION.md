# 1632. Rank Transform Of A Matrix

Use the standard efficient approach implemented in `matrixRankTransform`. The implementation handles the required edge cases deterministically.

Time and space complexity follow the algorithm's maintained state and input traversal.
