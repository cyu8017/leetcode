# 1615. Maximal Network Rank

Use the standard efficient approach implemented in `maximalNetworkRank`. The implementation handles the required edge cases deterministically.

Time and space complexity follow the algorithm's maintained state and input traversal.
