function goodNodes(root: any): any { const dfs = (node: any, maximum: any): any => !node ? 0 : (node.val >= maximum ? 1 : 0) + dfs(node.left, Math.max(maximum,node.val)) + dfs(node.right, Math.max(maximum,node.val)); return dfs(root, -Infinity); }
