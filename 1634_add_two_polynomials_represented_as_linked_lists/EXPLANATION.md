# 1634. Add Two Polynomials Represented As Linked Lists

Use the standard efficient approach implemented in `addPoly`. The implementation handles the required edge cases deterministically.

Time and space complexity follow the algorithm's maintained state and input traversal.
