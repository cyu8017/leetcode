# 1604. Alert Using Same Key Card Three Or More Times In A One Hour Period

Use the standard efficient approach implemented in `alertNames`. The implementation handles the required edge cases deterministically.

Time and space complexity follow the algorithm's maintained state and input traversal.
