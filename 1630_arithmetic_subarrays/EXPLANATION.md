# 1630. Arithmetic Subarrays

Use the standard efficient approach implemented in `checkArithmeticSubarrays`. The implementation handles the required edge cases deterministically.

Time and space complexity follow the algorithm's maintained state and input traversal.
