# 1605. Find Valid Matrix Given Row And Column Sums

Use the standard efficient approach implemented in `restoreMatrix`. The implementation handles the required edge cases deterministically.

Time and space complexity follow the algorithm's maintained state and input traversal.
