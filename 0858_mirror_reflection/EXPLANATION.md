# How We Solve Mirror Reflection

Unfold the room; reduce `p` and `q` by their GCD and read parities.

## Steps

1. Divide `p` and `q` by `gcd(p, q)`.
2. Even `p` → receptor 2; even `q` → receptor 0.
3. Otherwise receptor 1.
