# Approach
Track the best alternating sum ending with an even/odd index in the chosen subsequence. For each value, try adding it as a plus (even) or minus (odd) term.

# Complexity
Time O(n). Space O(1).
