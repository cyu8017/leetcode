# 1641. Count Sorted Vowel Strings

Use the standard efficient approach implemented in `countVowelStrings`. The implementation handles the required edge cases deterministically.

Time and space complexity follow the algorithm's maintained state and input traversal.
