# Approach
Track counts of special subsequences ending as `0+`, `0+1+`, and `0+1+2+`. Each new matching digit can either extend an existing same-ending subsequence (double) or append to the previous stage.

# Complexity
Time O(n). Space O(1).
