# Approach
Find the unique overall root (value appearing once). Attach other BSTs by replacing matching leaves with their root trees, then verify the result is a valid BST and every tree was used.

# Complexity
Time O(n). Space O(n).
